import { PxmConfig } from './types';

const config: PxmConfig = {
  animationEngine: 'vanilla', // 'gsap' or 'vanilla'
  defaults: {
    duration: 0.3,
    easing: 'ease-in-out',
  }
};

export default config;
